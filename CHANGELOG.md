# Changelog

## Unreleased

- converged publication contracts across the full 55-template Gallery, including distribution raw-point density, bounded ECDF markers, hexbin continuous count colorbars, and quiver boundary protection;
- rebuilt Mantel as a deterministic matrix/coupling composition with a collinear SourceRail, shared curve grammar, renderer-aware source packing, scatter-derived nodes, square primary geometry, and compact global vertical Colorbar behavior;
- added Primary Visual Area protection so measured ornaments reserve only their required physical strips and registered square scientific content is maximized deterministically;
- formalized the Agent execution protocol from natural-language scientific requests through clarification, direct/precomputed checks, minimal Figure Intent, validated rendering, and provenance return;
- added a 72-case Agent protocol benchmark specification with structural validation across all 55 public templates plus clarification, precomputed-result, and unsupported-scope cases; no LLM routing accuracy is claimed until those prompts are actually executed against an Agent;
- clarified the two-part installation boundary: the repository checkout owns the Agent Skill documents and recommendation knowledge, while the Python package owns the deterministic runtime and CLI;
- documented that registered `layouts/*` currently provide validated canonical composition fixtures only; user-data multi-panel composition through nested Figure Intents remains deferred.

## 1.0.0 — 2026-08-30

- established a 55-template, 13-family scientific taxonomy and registry-derived Gallery;
- added deterministic layout, ornaments, runtime/PDF validation, and semantic color;
- added minimal Figure Intent with explicit mapped-data adapters for all 55 public templates;
- added a progressive-disclosure Template Knowledge Base and concise Agent Skill routing;
- added a 55-case true external-data Evaluation with separate routing, canonical rendering,
  external rendering, runtime validation, repeatability, and Gallery coverage metrics;
- verified bundled fonts/licenses, styles, LaTeX resources, isolated wheel installation, and
  focused Python 3.11/3.12 CI.
