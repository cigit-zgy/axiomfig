# AxiomFig v1.1.0 release-candidate verification

## 结论

**Verdict: READY。** 本轮在候选提交
`3bf97594cba3f3d8fbe1e70540b53613212e232c` 上完成了范围冻结后的发布候选验证。没有发现需要
修改 runtime、Figure Intent、模板合同、视觉实现或文档的客观缺陷；唯一仓库变更是本报告。

本结论只表示该 SHA 通过 v1.1.0 release-candidate gates。版本仍为 `1.0.0`，本轮未创建或移动
tag，也未创建 GitHub Release。正式发布仍需要单独授权，并在授权后完成版本号与 Changelog 的
发布定稿。

## 提交范围

- Baseline SHA: `3bf97594cba3f3d8fbe1e70540b53613212e232c`
- Verified candidate SHA: `3bf97594cba3f3d8fbe1e70540b53613212e232c`
- Final runtime/documentation SHA: `3bf97594cba3f3d8fbe1e70540b53613212e232c`
- Report-only commit: 见本报告后的 Git history；报告提交不改变已验证候选内容
- Runtime/visual repair: 无

## Roadmap 审计

| 项目 | 状态 | 当前边界 |
|---|---|---|
| Agent Execution Protocol | CLOSED | `references/agent-protocol.md` 在最终模板选择前处理数据、科学意图、澄清和 direct/precomputed gate |
| Natural-language benchmark specification | CLOSED | `72` 条规范案例及结构验证器存在；只验证规范/合同覆盖 |
| 实际 LLM routing accuracy | UNMEASURED | 本轮及当前仓库均未执行真实 LLM benchmark，不声明模型准确率 |
| Multi-panel Figure Intent | FROZEN PARTIAL | `layouts/*` 是 canonical layout/gallery fixture；通用 user-data nested multi-panel composition 不属于冻结 v1.1 scope |
| Skill/runtime installation | CLOSED | checkout/Skill 提供 `SKILL.md` 与 references；wheel 提供 deterministic runtime、CLI、资源和 family contracts |
| v1.1 readiness contracts | CLOSED | `CONTRIBUTING.md` 已冻结 candidate gate；`CHANGELOG.md` 有 Unreleased 记录；版本和发布动作留给单独授权阶段 |

架构所有权未变化：`SKILL.md` 仍是短 router；`templates/index.yaml` 只负责 discovery；family
`contract.yaml`/`adapter.py` 负责科学 I/O；Figure Intent 是唯一 LLM/runtime 边界；视觉参数继续由
style、layout、ornament 和 typography runtime 决定。

## Release gates

| Gate | 状态 | 证据 |
|---|---|---|
| Ruff lint | PASS | `ruff check .` |
| Ruff format | PASS | `ruff format --check .`，`152 files already formatted` |
| Skill validator | PASS | `python scripts/validate_skill.py` |
| Agent protocol validator | PASS | `python -m tests.evaluation.agent_protocol`，`72` cases，`0` LLM runs claimed |
| Full pytest | PASS | `python -m pytest -q`，`376 passed in 126.11s` |
| Release Evaluation | PASS | `55/55` routing、canonical、external、runtime、artifact；repeatability `7/7`；Gallery coverage `55/55` |
| Registry-driven Gallery rebuild | PASS | `118` PDF + `118` PNG；重建前后 SHA-256 manifest 完全相同 |
| Gallery artifact validation | PASS | `axiomfig-validate gallery`，`118` PASS |
| Gallery visual inspection | PASS | 全部 `58` sans、`58` serif、`2` technical pairs 以 family contact sheets 在单栏尺度复核；可疑项单独打开 |
| Bundled fonts | PASS | `python scripts/check_fonts.py` |
| Tectonic/PDF/font probe | PASS | `python scripts/check_latex.py`；Tectonic 输出有效、字体嵌入、无 Type 3 failure |
| Wheel build | PASS | clean clone 构建 `axiomfig-1.0.0-py3-none-any.whl` |
| Wheel contents | PASS | `115` entries；无 tests、evaluation、reports、Gallery、references 或 `SKILL.md`；所需 package resources/contracts 存在 |
| Isolated installation | PASS | 不继承 system site-packages 的新 venv 完成依赖与 wheel 安装 |
| Clean-clone installation | PASS | GitHub clean clone HEAD 与 candidate SHA 一致后构建并安装 |
| Installed CLI E2E | PASS | wheel 安装后从 clone 外运行 parity Figure Intent，生成并验证 PDF/PNG |
| Public-template contract | PASS | `55` public，`13` families，`28` direct + `27` precomputed；registry 总数 `59` 含 `4` non-public layouts |
| Gallery-pair contract | PASS | `118` expected/actual pairs，无缺失或 orphan |
| Immutable v1.0.0 | PASS | annotated tag object `8a6b862...`，peeled commit `8c70caba07fc020962676f9344893de756aa5cba`，local/remote 一致且未修改 |
| GitHub CI candidate | PASS | run `33325029883`；candidate SHA；Python 3.11 与 3.12 jobs 均 success |

没有必需门禁处于 `FAIL` 或 `NOT VERIFIED`。

## 实际执行命令

```text
git fetch origin
git pull --ff-only origin master
ruff check .
ruff format --check .
python scripts/validate_skill.py
python -m tests.evaluation.agent_protocol
python -m pytest -q
axiomfig-gallery --gallery gallery --work-root <temporary-work-root>
axiomfig-validate gallery
python scripts/check_fonts.py
python scripts/check_latex.py
python scripts/evaluate_release.py --output tmp/v1.1-rc-evaluation
python -m pip wheel <clean-clone> --no-deps --no-build-isolation --wheel-dir <wheelhouse>
python -m zipfile -l <wheel>
python -m venv <isolated-venv>
<isolated-venv>/bin/python -m pip install <wheel>
<isolated-venv>/bin/axiomfig-intent intent.yaml --data data.csv --output parity
<isolated-venv>/bin/axiomfig-validate parity.pdf
gh run view 33325029883 --json databaseId,headSha,status,conclusion,url,jobs
git rev-list -n 1 v1.0.0
git ls-remote origin refs/tags/v1.0.0 refs/tags/v1.0.0^{}
```

最初两次安装 harness 分别因非交互 shell 中未设置 `python` PATH，以及使用了错误的 CLI flag
`--intent`/`--png` 而中止；第三次按实际 `--help`/README 合同，在不继承 system site-packages 的
全新 venv 中完整通过。两次失败均为审计脚本假设错误，不是仓库缺陷。

## Gallery 与视觉结论

完整 Gallery rebuild 没有改变任何 PNG/PDF blob。所有 family 的 sans/serif contact sheets 和两个
technical artifacts 均实际打开检查。未发现新的 clipping、overlap、字体混用、Colorbar、legend、
axes 或 panel geometry P0/P1 defect。

已有两项局部 P2 仍可复现并维持 deferred：

1. `ordination/pca_biplot` loading label clearance 偏紧；
2. `omics/volcano` canonical fixture 的纵向数据占用偏低。

它们未构成科学错误、合同违反或安装/执行失败，本轮按 scope freeze 未调整视觉或 fixture。

## 安装证据

clean clone SHA 与 candidate 相同。隔离安装报告：

- installed version: `1.0.0`（符合本轮禁止提前 bump 的要求）；
- public templates: `55`；
- packaged style/fonts/colors contracts 均可加载；
- parity PDF: `7380` bytes；
- parity PNG: `46572` bytes；
- wheel SHA-256: `6f293e4c064e6e34c23152650a8c3e34f20282a7d536a4ec1701a8d99d9b9d9d`。

## 最终判断与下一步

Release-candidate verdict: **READY**。

当前没有验证 blocker。下一项有界任务必须是一次单独、明确授权的 v1.1.0 release preparation：只
更新 package version、把 `Unreleased` 定稿为 `1.1.0`、重新执行发布门禁，然后才可创建 tag 和
GitHub Release。通用 user-data nested multi-panel composition、真实 LLM accuracy benchmark、PCA
label 和 volcano fixture 均不应被夹带进该发布任务。
