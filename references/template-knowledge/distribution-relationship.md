# Distribution and relationship routing

Use `scatter.*` for continuous-versus-continuous relationships and `scatter.parity` for observed
versus predicted values. Use hexbin only when overplotting makes individual points unreadable. Use
violin, box, ECDF, strip, or raincloud when the distribution itself matters; do not replace an
important distribution with mean-only bars. Bubble size must encode an explicit quantitative role.

