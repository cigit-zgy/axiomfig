family: association
variants:
  mantel:
    required: [correlation_matrix, matrix_labels, links, link_strength, significance]
    optional: [node_labels, strength_label]
  correlation_network:
    required: [nodes, edges, edge_weight]
    optional: [groups, significance, strength_label]
