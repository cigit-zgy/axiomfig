family: association
variants:
  mantel:
    required: [correlation_matrix, mantel_links, significance]
    optional: [matrix_labels, node_labels, strength_label]
  correlation_network:
    required: [nodes, correlation, threshold]
    optional: [groups, significance, strength_label]
