family: field
variants:
  contour:
    input_mode: direct
    required: [x_grid, y_grid, z, color_semantics]
    optional: [center, levels, colorbar_label, xlabel, ylabel]
  quiver:
    input_mode: direct
    required: [x, y, u, v, color_semantics]
    optional: [magnitude, colorbar_label, xlabel, ylabel]
