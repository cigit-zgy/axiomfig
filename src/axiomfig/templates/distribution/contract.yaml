family: distribution
variants:
  histogram:
    required: [value]
    optional: [bins, xlabel, ylabel]
  density:
    required: [x, density]
    optional: [group, xlabel, ylabel]
  ecdf:
    required: [value]
    optional: [group, xlabel, ylabel]
  box:
    required: [value, category]
    optional: [ylabel]
  violin:
    required: [value, category]
    optional: [ylabel]
  box_violin:
    required: [value, category]
    optional: [ylabel]
  strip:
    required: [value, category]
    optional: [jitter, ylabel]
  raincloud:
    required: [value, category]
    optional: [jitter, summary, ylabel]
