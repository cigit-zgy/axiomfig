---
artifact_type: codex_report
artifact_id: 260908_codex_01
task_id: AXIOMFIG-BAR-CONTRACT-260908-02
title: Bar endpoint uncertainty, sparse groups, and architecture falsification
date: 2026-09-08
project: AxiomFig
repository: cigit-zgy/axiomfig
status: completed
summary: >
  Implemented the accepted endpoint and sparse-grouped contracts, preserved cumulative
  complete grids, and verified real external data, installed packages, and Gallery artifacts.
  Independent falsification overturned the first green candidate; the minimal numerical
  repair passed a separately pinned review. Optional benchmark dependencies and one
  pre-existing out-of-scope association documentation mismatch remain disclosed.
verdict: PASS_WITH_LIMITATIONS
task_branch: task/260908-bar-contract-hardening
task_source_sha: d66a5b22406aa5dec1e2f6a3b3bffa0326a94cb2
baseline_sha: 37711e908d91929c5c54cc5d97e08db0ba0a4179
final_task_head: c3b9e597b7b3533082cec24c066e4374d0ab28e8
upstream_task_head: c3b9e597b7b3533082cec24c066e4374d0ab28e8
task_head_equals_upstream: true
head_scope: verified_source_candidate_before_evidence_only_publication
verification_level: level_3
limitations:
  - Optional historical layout-comparison dependencies adjustText and textalloc were not installed; their two local tests skipped.
  - Hosted fast CI excludes E2E and lacks pdffonts; complete artifact/font/package gates ran locally on Python 3.11.
  - Pre-existing association YAML-dataset wording in references/figure-intent.md remains at its out-of-scope projection owner; unsupported datasets fail bounded.
---

# Bar contract implementation — Codex execution report

This is execution evidence, not ChatGPT acceptance. The sole task specification is
[`d66a5b2:reports/chatgpt/260908_chatgpt_02.md`](https://github.com/cigit-zgy/axiomfig/blob/d66a5b22406aa5dec1e2f6a3b3bffa0326a94cb2/reports/chatgpt/260908_chatgpt_02.md).
The older task's master-only workflow is superseded; no default-branch integration was performed.

## Changes

ChatGPT DIRECT work already present at handoff established `AGENTS.md`, the current `design/`
authority, Bar/routing projections, historical-report migration, and the dedicated task branch.
Codex did not re-author those semantics or claim that migration as its implementation.

Codex commits:

| Commit | Change |
| --- | --- |
| `917c15161ed94bdf67449f541e6d5cb400ac2fc1` | Implement endpoint uncertainty and sparse grouped rows; add external examples, artist/property tests and architecture checks. |
| `ca2d158db870721db08a0ab2ecafcef506b8a240` | Repair installation-test isolation from inherited Python source paths. |
| `c3b9e597b7b3533082cec24c066e4374d0ab28e8` | Bound Bar tick-candidate overflow before rendering; add 24 regressions and preserve failed independent evidence. |

The final source candidate is `c3b9e59`; this report and the second independent artifact are
evidence-only publication additions. The report-containing commit is identified by the immutable
completion locator, rather than a fabricated self-referential SHA inside its own file.

- `bar.simple` / `bar.grouped` accept paired `lower`/`upper`, require explicit uncertainty meaning,
  enforce enclosing finite/renderable endpoints, and exclude simultaneous `error`.
- The existing adapter converts endpoints into declared error widths. Its output may normalize
  optional roles, but must retain required roles and cannot introduce undeclared roles.
- Sparse grouped rendering iterates supplied rows with first-seen category/global-group order
  and fixed global slots. Missing keys have no patch; supplied zero retains a patch. No fill,
  aggregation, sorting or imputation was introduced.
- Stacked, normalized-stacked, grouped-stacked, diverging-stacked and mirrored retain complete
  grids. Registered within-category plotting normalization remains unchanged and tested;
  scientific normalization remains upstream.
- New permanent external examples are `examples/bar/simple_interval.{csv,intent.yaml}` and
  `examples/bar/grouped_sparse_interval.{csv,intent.yaml}`. Existing error-width examples remain.
- No new grammar, backend, dependency, Figure Intent schema, low-level visual field, style value,
  nested composition, version, tag or release. No edits to `SKILL.md`, references, living design,
  visual resources or formal Gallery after handoff. Bar's executable optional-role contract changed.

## AI-assisted implementation transparency

The main Codex agent implemented production changes and public-path regressions. A separate
architecture-probe agent authored six test-only files; it was not used as the independent reviewer.
Two later fresh-context reviewers attempted to falsify different committed candidates, without
reading a main implementation report. The second also delegated a bounded read-only surface check.

Observable engineering choices: reuse the existing adapter/builder/Bar geometry owners; test
real Matplotlib artists and external CLI results; preflight existing nice-axis arithmetic rather
than add an arbitrary numeric cutoff; catch only the identified numerical validation exceptions,
not all builder exceptions. No new abstraction or runtime dependency was necessary.

## Shared coding-Skill alignment

- Collaboration authority: `cigit-zgy/agent-collaboration@ad88170b23920ddac0bff9a2fd467aa0c59917cf`.
  Its SKILL and routed protocol/execution/formal, implementation, verification, shared coding,
  maintained-Skill, report and report-template owners were read at that revision.
- Activated shared coding Skill: `DietrichGebert/ponytail@2ed6c52c9d7e5e56942508591085fd45dea277d3:skills/ponytail/SKILL.md`, **full**.
  Resolved content and local cache matched Git blob `02c0712c86277d49d18a77da3a2b825657bf02d1`;
  both independent reviewers checked the same alignment. It constrained unnecessary abstractions,
  without overriding numerical/scientific safety or Level 3 verification.
- Host verification guidance consulted: `repository-audit` and its checklist,
  `superpowers/verification-before-completion`, `superpowers/systematic-debugging`.
  Project `SKILL.md` supplied AxiomFig's normal route. These were not additional benchmark inputs.
- `python-style: NOT PRESENT`; no replacement assumed. `modern-python` was not activated.

## Verification evidence

Final ordinary gates ran on committed `c3b9e59` before the second independent review. Healthy
existing runtime was reused: Python 3.11.16, NumPy 2.4.6, Matplotlib 3.10.9, Ruff 0.16.5,
Tectonic 0.17.0, Poppler 26.08.0 and pip-audit 2.10.1. Only explicitly required disposable
wheel/checkout environments were created. No project or machine environment was rebuilt.

| Category | Actually verified result |
| --- | --- |
| Component/property | 193 focused Bar/architecture/hardening tests passed after the numerical repair; includes asymmetric/zero/tiny intervals, 24 red-to-green extreme-value regressions and sparse row/subset/order properties. |
| Scientific geometry | Grouped slots, missing vs zero, interval endpoints/containment, stacked bottoms, normalized totals, grouped-stacked hierarchy, independent diverging accumulation, range endpoints, mirrored sign and waterfall cumulative roles remain tested. |
| Architecture/invariants | Authority/design/report/archive separation; relative/alias cross-family imports; declared adapter outputs; guide-role/registry/builder parity; dynamic inventories; compatibility exclusions; strict/duplicate structured input; public-error and low-level visual boundaries passed. |
| Full local pytest | **841 passed, 2 skipped**, 179.54 s. No E2E deselection. Skips are the optional historical adjustText/textalloc comparisons, not production Bar or release-artifact paths. |
| Ruff / format / Mypy | PASS / PASS / PASS, 76 production source files. Historical root archive is excluded from current-code Ruff, not rewritten to satisfy its Markdown-code formatting. |
| Skill / Agent protocol | PASS; current corpus structural validator: **124 cases**, 74 render specifications. No real LLM routing run or accuracy claim. |
| Release Evaluation | **61/61** routing, canonical, external, runtime and artifact gates; zero failures; existing render-repeat sample **7/7**; curated Gallery coverage **68/68**. |
| Structural repeatability | **61/61** public templates, three in-process and two fresh-process runs, zero mismatches. Inventory comes from current registry, not a replacement magic count. |
| External data E2E | Nine core Bar CSV intents plus two new CSV examples, retained uncertainty example, and both new examples through JSON. Actual CLI PDF/PNG creation and validation passed. |
| Clean wheel | Non-editable install outside importable source; package resource/font discovery, canonical/parity and both new Bar CLI examples, actual PDF/PNG validation passed. |
| Clean checkout | New clone explicitly checked out at candidate, non-editable installation, documented parity plus both new Bar CLI examples and artifact validation passed. |
| Formal Gallery / fonts | Entire 68-pair serif family-first Gallery rebuilt and validated, including bundled-font/Tectonic/PDF checks. Same-backend rebuild of all 136 PDF/PNG files was byte-identical to committed Gallery. |
| Dependency audit | `python -m pip_audit .`: no known vulnerabilities. Cache deserialization warnings caused ignored cache entries, not a skipped audit. |

Registry observations: 13 scientific families, 61 public templates/adapters, 58 recommended
templates, 65 registered builders including four non-public layout fixtures. Nine core Bar
grammars remain; `bar.vertical`, `bar.horizontal`, `bar.dot` remain executable compatibility IDs,
not recommended core grammar or formal Gallery IDs. Curated inventory remains 68 pairs, including
16 Bar pairs. Public inventory and Gallery inventory are deliberately separate sources.

### Commands actually executed

Working directory was the canonical project root. `PATH` included the existing Miniforge and
Homebrew executables; `PYTHONPATH=$PWD/src` applied to source checks, with it and `PYTHONHOME`
removed **before installation and execution** inside isolated package tests. Bytecode writes
were disabled. `TMPDIR`, Matplotlib, Hypothesis, pip, Ruff, Mypy and pytest caches were confined
to the task's `tmp/AXIOMFIG-BAR-CONTRACT-260908-02/` tree.

```sh
ruff check .
ruff format --check .
python -m mypy src/axiomfig --cache-dir tmp/AXIOMFIG-BAR-CONTRACT-260908-02/cache/mypy
python scripts/validate_skill.py
python -m tests.evaluation.agent_protocol
python -m pytest -q --basetemp=tmp/AXIOMFIG-BAR-CONTRACT-260908-02/run/c3b9-full -o cache_dir=tmp/AXIOMFIG-BAR-CONTRACT-260908-02/cache/pytest
python scripts/evaluate_release.py --output tmp/AXIOMFIG-BAR-CONTRACT-260908-02/evaluation-c3b9
python -m tests.evaluation.structural_repeatability --output tmp/AXIOMFIG-BAR-CONTRACT-260908-02/run/c3b9-repeatability.json
python -m pip_audit .
python -c 'import matplotlib; from axiomfig.cli import gallery_main; print("BACKEND", matplotlib.get_backend()); raise SystemExit(gallery_main())' --gallery tmp/AXIOMFIG-BAR-CONTRACT-260908-02/renders/c3b9/gallery --work-root tmp/AXIOMFIG-BAR-CONTRACT-260908-02/renders/c3b9/work
diff -qr --exclude=README.md gallery tmp/AXIOMFIG-BAR-CONTRACT-260908-02/renders/c3b9/gallery
```

The full suite really executed `tests/test_clean_checkout_e2e.py` and
`tests/test_packaged_cli.py`, including venv creation, wheel build, pip installation with cleaned
environment, installed `axiomfig-intent` / `axiomfig-render` and `axiomfig-validate` commands.
The independent evidence below records separately reproducible adversarial commands.

### Gallery observation, not only metrics

Opened actual baseline/rebuilt `bar/grouped_uncertainty.png`; new `simple_interval.png` and
`grouped_sparse_interval.png`; rebuilt `bar/grouped_vertical.png` and `bar/grouped_horizontal.png`;
and an additional disposable serif horizontal sparse-endpoint render. The final candidate's
two external PNGs and the rebuilt grouped uncertainty/horizontal PNGs were opened again.

Observed asymmetric interval placement, uncertainty containment, stable empty slots and a visible
supplied-zero state, with no new label/legend clipping or collision. The existing formal figures
did not change. Baseline versus post-change Bar rebuilds were byte-identical. Pytest's Agg and
the local standalone measurement backend can produce small margin/byte differences from each
other; controlled comparisons used the same backend. No global layout retuning was undertaken.
These temporary comparison images are not new formal Gallery cases.

## Failures, classification, and independent falsification

| Finding | Classification and disposition |
| --- | --- |
| Prior grouped-incomplete rejection and fixed-tolerance prose assertion contradicted accepted handoff projections. | `TEST_DEFECT` / projection drift. Replaced only obsolete expectations; complete grids in the five other grammars and actual numerical checks remain enforced. Handoff CI `34171445170` failed on the prose assertion. |
| First full run: 1 failed, 816 passed, 2 skipped in wheel E2E. | Test-environment defect. Inherited `PYTHONPATH` let pip treat source metadata as an existing install. Removing overrides before pip, then asserting module origin under isolated `sys.prefix`, fixed the test's isolation. Both installation E2Es then passed. |
| Independent review disproved green candidate `ca2d158`: 24 finite Bar inputs leaked raw numerical exceptions. | `IMPLEMENTATION_DRIFT`, F1. Nice-axis candidate generation could overflow after finite-extrema checks. Added existing-axis preflight in Bar geometry and 24 failing-then-passing regressions. No blanket builder catch or arbitrary cutoff. |
| Ruff attempted to reformat Python snippets in retained historical evidence. | Tool/ownership mismatch. Excluded root `00_archive` from current-code lint/format; old evidence remains byte-for-byte unchanged. Active code/tests remain checked. |
| Association guide says structured JSON/YAML **data**, but `load_dataset` accepts CSV/JSON. | Existing low-severity, out-of-scope `PROJECTION_DRIFT`, L1. Same guide blob at handoff and final candidate. YAML datasets are rejected with bounded `FigureIntentError`; no silent scientific rendering. Returned to ChatGPT's projection owner, not repaired in this Bar task. |

Independent artifacts, preserved separately:

- [FAIL on ca2d158](../../00_archive/verification/AXIOMFIG-BAR-CONTRACT-260908-02/falsification-ca2d158.md),
  reviewer `/root/final_falsification`; SHA-256 `c4ee6c4185ffac1d8776a149429cfc2be6506cd86041b9103b6ea68fb0a73c22`.
- [PASS_WITH_LIMITATIONS on c3b9e59](../../00_archive/verification/AXIOMFIG-BAR-CONTRACT-260908-02/falsification-c3b9e59.md),
  fresh reviewer `/root/candidate_reaudit` plus separate surface inspection;
  SHA-256 `e8f67089ab5167cf9a13dbf3b87e06cac10bf73ae13df9f5cabe96a1de301ea4`.

The second review independently replayed F1 (24 bounded), challenged 3,000 seeded large-value
geometry trials (509 bounded, 2,491 finite actual-axis results), exercised 72 sparse artist/draw
cases and 72 complete-grid deletions, malformed/boundary/normalization probes and 26 architecture
tests. It checked all 16 committed Bar pairs. All 113 wheel runtime members matched candidate
Git blobs, then matched the installed files; isolated installed execution passed two sparse draws
and eight endpoint F1 cases. No task-level blocker or `DESIGN_GAP` remained.

**Evidence categories are not interchangeable:**

- **Local release gate verified:** all ordinary gates above executed by the implementation owner.
- **GitHub CI verified:** candidate run [`34173632739`](https://github.com/cigit-zgy/axiomfig/actions/runs/34173632739), exact `c3b9e59`, Python 3.11 and 3.12 both successful. Each fast suite: 823 passed, 3 skipped, 17 E2E deselected; each separate external-data path: 1 passed. Hosted CI is not the full local artifact gate. Local execution covered the hosted `pdffonts` skip.
- **Independent audit verified:** only the concrete independent scopes in the two candidate-pinned artifacts. The reviewers did not rerun all local release gates or independently certify hosted CI, report publication or cleanup.
- **Reported but not independently verified:** the full local suite/evaluation/rebuild and CI were observed by this execution owner, not rerun by the independent reviewer. No exhaustive floating-point/all-family proof or actual LLM accuracy is claimed.

Earlier run `34172317110` was cancelled when superseded. `34172609527` passed on `ca2d158`, but
was correctly rejected as sufficient evidence once independent F1 was found. The evidence-only
report publication must also pass its exact-head CI before the final completion locator is sent;
its Checks are attached to the immutable report-containing commit.

## Local temporary state

Only `tmp/AXIOMFIG-BAR-CONTRACT-260908-02/` was used for owned scratch, caches, renders,
disposable package environments, fresh clones and independent probes. No linked worktree was
needed: the initial primary checkout was clean. No User file, environment or unrelated worktree
was reset, stashed, overwritten or deleted.

Scratch disposition: after the report conformance check (25 tests plus Ruff/format passed),
removed only this owned 1.9 GB disposable tree and its then-empty root `tmp/`; absence was checked.
Durable failed/successful audit evidence remains above. Temporary logs/renders/environments are
regenerable from the recorded commands, not retained as undeclared recovery state. No raw model
output, comparison Gallery or disposable environment is part of the deliverable.

## Acceptance criteria / blockers

All in-scope implementation, scientific, architecture, artifact and candidate-verification
criteria are satisfied, subject to final evidence-only publication checks. The named optional
comparison skips and existing association wording drift justify **PASS_WITH_LIMITATIONS**, not
an unqualified PASS. There is no unresolved Bar task blocker or invented scientific design.

Normal discovery did not expand: `SKILL.md` remains 4,035 bytes and registry 3,228 bytes; current
representative release discovery estimate is 1,994 approximate tokens. The Bar contract is
1,655 bytes after adding the accepted endpoint roles. No live Agent benchmark was run.

## Git synchronization result

**PRE:** canonical origin verified as `https://github.com/cigit-zgy/axiomfig.git`; fresh fetch
resolved the issued task branch to `d66a5b22406aa5dec1e2f6a3b3bffa0326a94cb2`. Clean local
execution HEAD was established at that same handoff before mutation. Metadata baseline
`37711e908d91929c5c54cc5d97e08db0ba0a4179` is its verified ancestor, not an alternative execution
starting point. Pre-existing local master state was preserved; no destructive synchronization.

**POST source verification seal:** after commit/push and fresh fetch,
local HEAD = `origin/task/260908-bar-contract-hardening` = fresh GitHub task head =
`c3b9e597b7b3533082cec24c066e4374d0ab28e8`. `TASK_HEAD_EQUALS_UPSTREAM = YES`.
Every implementation push used a new pre-push fetch and post-push equality check.

The YAML head fields record this exact tested-source snapshot, before evidence-only publication.
The final report-containing SHA is resolved by the required immutable completion locator;
publication repeats push → fresh fetch → exact local/upstream/GitHub equality, verifies clean
working tree and exact-head CI before returning that locator. This avoids pretending the report
can already contain its own future commit hash. No source/test/config change follows the seal.

Remote master remains `e554072fb1c16927b3b093e035aa72e3b19db33d`; integration is left to ChatGPT
after acceptance under the task's AUTO policy. Version stays 1.1.0. Local and remote tag object /
peeled target checks agree and remain unchanged:

| Tag | Tag object | Target commit |
| --- | --- | --- |
| v1.0.0 | `8a6b862ad9793e86981ea45d0b99bb5539c2da26` | `8c70caba07fc020962676f9344893de756aa5cba` |
| v1.1.0 | `f92d9b5a92f69d69f0edb8b9b6efb6af9494e000` | `45ec3dd50156d76d35c3d49ddbd95a74a7aea420` |
